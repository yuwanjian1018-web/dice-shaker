# 好运骰盅清新版 · 设计与交互验收

final result: passed

验收日期：2026-08-30（本机时区）。范围为原生微信小程序单页设计及核心交互；没有转换成网页，没有上传或发布。

## 视觉依据与对照证据

- 原始视觉依据：`design/before-revealed.png`；用户要求与本次改版约定：`design/brief.md`。
- 实现截图：`design/after-closed-initial.jpg`、`design/after-open-preserved.jpg`。
- 全画面对照：`design/comparison-before-after.png`，左为原版自动揭晓，右为新版手动开盖；两边随机数不同，不把骰面数值差异视为设计偏差。
- 局部对照：`design/comparison-card-detail.png`，同一坐标裁切主卡片并放大两倍，用于检查骰点、图像边缘、按钮图标、对齐和文案。
- 新版开合预览：`design/preview-closed-open.png`，左为合盖、右为开盖。
- 恢复原机型后的最终工作窗口：`design/final-wechat-preview.jpg`；最终控制台：`design/console-final.txt`（0 Errors，2 条工具/基础库提示）。该窗口中手机位置随侧栏宽度移动，不替代上述同坐标对照截图。
- 已把原版与新版放在同一张全画面对照和同一张细节对照中查看；不是仅从代码或分开查看的截图判断。

原截图和实现截图均为 1252 × 1000 像素。原截图虽名为 `.png`，实际为工具返回的 JPEG，保留原件不修改。对应 iPhone 15 Pro Max 预设为 430 × 932 CSS 像素、DPR 3；模拟器在桌面约以 84% 显示。对照统一裁切 `left=822, top=174, width=360, height=678` 的小程序内容，排除桌面工具、原生导航栏与底部指示条。卡片局部裁切为 `837,284,330,440`，只等比放大。元数据见 `design/comparison-metadata.json`。

这是对既有小程序的定向改版，不是像素复刻。原版暗色、酒红与总分区域被暖白、薄荷绿、浅木托盘和开合按钮替换，均为用户要求的有意变化。保留五颗骰子的原生点阵组件、单页结构、微信导航和运行环境。

## 发现、修复与复验记录

| 轮次 | 发现及严重度 | 修复 | 复验证据 |
| --- | --- | --- | --- |
| 初次模拟器检查，未通过 | P1：微信原生按钮默认宽度及自动边距覆盖骰盅定位，合盖时偏右 | 在骰盅组件中明确覆盖宽度、边距、内边距，并上移闭合基线，让杯沿落在托盘内 | `after-closed-initial.jpg`；开合预览左图 |
| 开盖检查，未通过 | P1：打开后盖子仍挡住右上方骰面 | 调整向右上方移动距离和缩放比例，五颗骰面完全露出且盖子仍可点按 | `after-open-preserved.jpg`；`comparison-card-detail.png` |
| 动作检查，未通过 | P2：禁用期间原生 button 出现白色矩形背景；属性和标签选择器产生 WXSS 警告 | 忙碌样式改为类选择器；骰盅背景明确保持透明；提示图标使用独立类名 | `after-opening.jpg`；`console-after-compile.txt` |
| 文字检查，未通过 | P2：原拟定浅灰绿辅助文字对比度不足 | 加深辅助文字、状态标签颜色；保留浅底与轻阴影 | 全画面对照、局部对照及下方对比度记录 |
| 最终同图对照，通过 | 已检查整体及主卡片，未发现仍需修复的 P0/P1/P2 视觉问题 | 无新增视觉修改 | `comparison-before-after.png`、`comparison-card-detail.png` |
| 小屏幕与核心流程，通过 | 375 × 667 上按钮及骰面完整可见，页尾可滚动查看；开合、摇动流程可用 | 恢复原来机型后交付 | `small-375-closed.jpg`、`small-375-open.jpg`、`small-375-bottom.jpg` |

初轮问题依据本次会话中即时显示的模拟器截图；这些问题帧没有全部独立落盘。上表的文件为修复后的可复查证据，不冒充修复前截图。资源尚未构建齐全时出现过图标缺失报错，完整构建并重新编译后已消失；这一项属于构建问题，不计为视觉迭代。

## 五项必检表面

| 表面 | 结论与具体检查 |
| --- | --- |
| 字体与排版 | 保留系统中文字体回退栈；标题 54rpx、主按钮 28rpx、状态文字 25rpx。两种视口中标题、按钮与状态文字无截断和异常换行。没有引入需远程下载的字体。 |
| 间距与布局 | 单页横向留白 32rpx；场景 540rpx；五骰以 3+2 排列。合盖居中，开盖后图像和骰面不相交。375 × 667 首屏可见全部主操作，少量底部说明通过正常页面滚动查看，不固定裁切。 |
| 颜色与状态 | 暖白底、薄荷绿主色、浅木托盘。一和四为红点，其余为蓝点；通过点阵数量识别数值，不单靠颜色。状态标签、禁用状态及主要操作层级清晰。 |
| 图像质量与来源 | 骰盅和托盘为 ImageGen 透明 PNG，保留生成原图，运行素材等比缩小。无拉伸、硬白矩形底或可见锯齿问题。按钮图标采用 Phosphor 官方素材并保留 MIT 许可。原生骰点组件沿用原项目，没有用新 CSS 图画去替代应有的图片素材。 |
| 文案与内容 | 已移除总点数、合计文案及相关状态字段。按钮明确表示“摇一摇 / 打开盖子 / 合上盖子”；摇完提示“摇好了，打开看看吧”。没有残留自动揭盖的提示。 |

从样式色值计算的对比度（非截图抗锯齿像素测量）：辅助文字 `#617367` 对 `#f5f8f2` 为 4.71:1；卡片辅助文字 `#65766a` 对白色为 4.82:1；状态文字 `#4f7263` 对 `#edf4ed` 为 4.79:1；主按钮白字对 `#427862` 为 5.13:1；次按钮文字对背景为 6.00:1。红、蓝骰点对白色分别为 3.84:1 和 4.54:1。

## 实际运行验收

环境：微信开发者工具 Stable 2.01.2510260，基础库 3.16.2。

| 场景 | 实际结果与证据 |
| --- | --- |
| 初始合盖，点击骰盅打开 | 显示初始 `[1,2,3,4,5]`；一、四红点，二、三、五蓝点；`after-open-initial.jpg`。 |
| 独立按钮合盖，再点骰盅打开 | 仍为 `[1,2,3,4,5]`；`after-open-preserved.jpg`。 |
| 从开盖状态连续点击摇一摇 | 先合盖再摇动，最终保持合盖；后续再次观察仍提示“摇好了，打开看看吧”；`after-roll-stays-closed.jpg`。 |
| 手动揭晓新一轮 | 观察到新结果 `[1,1,1,3,1]`，与摇动前 `[5,3,1,5,3]` 不同；`before-manual-roll.jpg`、`after-manual-reveal.jpg`。 |
| 新一轮点击悬起的骰盅合盖，再按按钮打开 | 结果仍为 `[1,1,1,3,1]`；`after-manual-reopen.jpg`。 |
| 快速重复点击开盖 | 双击后保持打开，未出现两个重叠流程；精确随机数调用次数和忙碌锁由自动化测试验证。 |
| 375 × 667 小屏幕 | 合盖、打开、点按及页尾滚动通过，主按钮没有遮挡；小屏截图见上。预设 DPR 2，桌面截图中手机显示区域约 383 × 681 像素，不与大屏截图作逐像素比较。 |
| 控制台 | 最终稳定运行时 0 Errors。剩余日志为开发工具热重载提示与基础库兼容性提示；某次编译另有 Chromium SharedArrayBuffer 弃用提示。没有残留本地素材错误或本页 WXSS 选择器警告。 |

## 自动化与包体检查

- `npm.cmd test`：12 项通过、0 失败。覆盖六种点阵和红蓝映射、手动开合保留点数、摇后保持合盖、动作互斥、页面退出清理计时器、页面事件连接。
- `npm.cmd run check`：通过。检查页面四类文件、4 个业务 JS 文件语法、6 个本地素材引用、WXML 与 WXSS 结构。日志保存于 `design/test-results.txt`、`design/check-results.txt`。
- 运行资源共 572,330 字节（约 559 KiB，含 7 个 PNG/许可文件）；这不是开发工具上传包体的实测值。
- `design/`、`scripts/`、`tests/`、README 与本验收文件从小程序打包中排除；原始生成素材不随应用分发。
- AppID、账号、安全、代理设置没有改动；没有提交 Git、上传小程序或发布。

## 剩余边界

- 尚未在实体手机上测试；不同手机系统、字体缩放、读屏器及低性能设备的动画仍需真机复测。
- 模拟器验证了 375 × 667 和 430 × 932，未声称覆盖全部机型。
- 六点蓝色与六种点阵已经自动化验证，本轮留存的模拟器截图没有包含六点骰面。
- 当前随机数适用于娱乐展示，不用于资金、奖品兑付等需要可审计随机性的场景。

## 实现清单

- [x] 清新配色和本地透明素材
- [x] 手动打开与合上，骰盅和按钮双入口
- [x] 摇动结束保持合盖，开合不重新掷骰
- [x] 红蓝骰点，移除总点数
- [x] 动作锁定、生命周期清理与回归测试
- [x] 大小两种模拟器视口验收、同图视觉对照
- [x] 原图、来源、测试日志与验收截图留痕
