{
  "name": "dice-shaker-miniprogram",
  "version": "1.0.0",
  "private": true,
  "description": "A native WeChat Mini Program that rolls five dice",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-project.js"
  }
}
