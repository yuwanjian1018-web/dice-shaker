{
  "name": "dice-shaker-miniprogram",
  "version": "1.0.0",
  "private": true,
  "description": "Native WeChat dice shaker with black cup, sound, lock and 1–6 dice",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-project.js",
    "build:audio": "node scripts/build-shake-audio.js"
  }
}
