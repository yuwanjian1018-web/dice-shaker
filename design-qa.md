# 当前版本清理与验证记录 · 2026-09-08

## 清理边界

清理对象仅限当前 3D 实现不再引用的图片版设计、中间模型、对比截图、旧验收输出和对应构建脚本。保留以下内容：

- `pages/`、`utils/`、`assets/`、`vendor/` 中的全部当前运行文件
- `design/cup-model-v3/` 中的当前 Blender/GLB 源、遮蔽缓存和验证记录
- 自动化测试、项目检查、3D/音效重建脚本和开发者工具 QA 脚本
- Git 历史、现有 AppID、个人开发者工具配置和所有账号/网络设置

删除的主要目录包括历史概念图、黑色图片版实现、旧素材源、旧布局/透视/点数迭代、独立骰子建模分支，以及 `cup-model-v3/previous-images/`。同时删除只重建旧图标/纹理的两个脚本，并清除不再需要且被 Git 忽略的 `node_modules/`。

共删除 249 个 Git 跟踪文件（约 84.95 MiB）和约 16.05 MiB 的 `node_modules/`，工作目录合计释放约 101 MiB。当前 `design/` 只保留约 18.29 MiB 的 `cup-model-v3/`。

## 验证结论

- 清理前：`npm.cmd test` 33/33 通过；`npm.cmd run check` 通过。
- 清理后：`npm.cmd test` 33/33 通过；`npm.cmd run check` 通过。
- 项目检查确认 14 个页面素材引用有效，图片与音频合计 194.3 KiB，本地待打包文件约 1.37 MiB，均在项目门限内。
- 本轮只完成源码和自动化回归；没有把模拟器结果表述为真机性能、体感或发布审核结论。

## 恢复方式

已跟踪文件的删除会显示在 Git 差异中，可在提交前直接撤销，也可从此前提交恢复。`node_modules/` 未被 Git 跟踪，但当前项目测试和运行不依赖其中的软件包。
