# 代码质量与冗余清理 · 2026-09-08

## 13:24 扫描后修正：媒体总量限制

用户提供的新截图显示「图片和音频资源」仍未通过。此前按单文件检查是错误理解。已只读核对当前安装的开发者工具 `resources/app.asar` 中 `js/3b6b93029b234f57c8b150f8796c7aef.js` 的 `checkImageAndAudioSizeLimit`：遍历 `compiledPkg`，按媒体扩展名累计所有文件长度，以 `total / 1024 < 200` 判定，属于合计且严格小于的限制。

- 将原 105884 字节 WAV 音效编码为 7548 字节 MP3（44.1 kHz、单声道、48 kbps）；解码/ffprobe 确认时长 1.2 秒。合成算法保持原样，构建脚本使用 FFmpeg 重建 MP3，原 WAV 移至项目外验收目录保留。
- 图片和音频合计由 297309 字节（290.3 KiB）降至 198973 字节（194.3 KiB）；主包本地待打包估算为 1393.3 KiB。此次没有继续降低贴图质量或改变模型。
- 检查器改为遍历待打包文件的媒体合计，并拒绝 `>= 200 KiB`；已修正 README 的错误口径。
- 30 项测试全部通过。刷新模拟器后，实际音频上下文报告 MP3、duration=1.2、摇动中 playing=true、onPlay=1、soundError=false；结束后 paused=true、phase=covered，模型正常；最终 error 日志检索无匹配。
- 以上为本地规则核验与模拟器验证，尚未取得修正后代码质量面板的新截图。

## 第一轮清理记录（媒体阈值解释已由上文修正）

当前状态：本地质量门禁、30 项自动测试、24 项模拟器交互检查通过。WXML/WXSS 编译通过，模型加载成功，最终 console 的 error 检索无匹配。尚未重新获取开发者工具「代码质量」面板的扫描结果；编译后最终包大小与真机 GPU/声音/体感表现不由本地估算代替。

- 已启用 `lazyCodeLoading: requiredComponents`。本地待打包大小从 1801.5 KiB 降至 1489.4 KiB；检查预算改为 1536 KiB，并新增无引用业务 JS、200 KiB 图片/音频检查。
- `cup-scene.bin` 从 697389 字节降至 551812 字节：只合并完整属性字节相同的顶点，并移除纯色材质未使用的 UV。完整性测试与源 GLB 逐三角形角点比对位置、法线、有效 UV，并核对烘焙遮蔽及动画采样；全部 26316 个三角形保留。
- 法线贴图从 328512 字节降至 160365 字节，保持 1024×1024 和无色度抽样。已查看合盖前后、开盖及设置面板截图，未发现明显材质退化。其余贴图、声音文件保持原样。
- 删除旧图片布局模块/测试、五个历史素材/对比脚本、固定端口截图脚本与不再需要的 npm 锁文件。移除 `miniprogram-automator` 依赖及约 16 MiB 的 `node_modules`；移除后所有测试仍可直接用 Node.js 运行。
- 删除状态中的旧点阵、颜色与闲置文案。重复拖动位置不再发送更新；3D 骰子的位姿仅在结果/数量变化时更新；合盖时长统一使用游戏模块常量。随机数非数值输入现会被拒绝。
- 修复体感验收脚本对旧 `.die` / `.dice-tray--covered` 及不存在的 `die.position` 的引用，改为验证实际 3D 场景。24 项模拟器检查覆盖按钮/体感互斥、锁定、设置、后台生命周期、开合保持布局与 1–6 颗渲染；加速度注入不等于真机硬件测试。
- 本次新增删除的源文件和移出的依赖可从项目外 `C:/Users/zhout/Documents/ChatGPT/dice-shaker_V1/verification/quality-cleanup/` 恢复，该目录也保留优化前模型及前后截图。进入本次任务前就已删除的图片未计入本次清理成果。历史设计和完整模型作为来源/重建输入继续保留。

以下为历史验收记录，不代表当前 3D 实现。

---

# 骰盅放大与源码包清理 · 2026-09-06

当前状态：代码与本地检查通过；清理后的真机调试包上传成功，手机端运行尚未验证。

## 当前修改

- 上杯显示尺寸从 492 × 598rpx 增大至 532 × 646rpx（约 8%）；仍以底边定位，托盘、骰子与前沿在杯体后方。正在使用的 PNG 文件未改。
- 按用户追加要求，最大抬升从 308rpx 增大至 340rpx，优先让骰子容易查看，允许杯顶超出屏幕。保留原短屏布局；不为显示完整杯顶缩小整体场景。
- 删除 14 个未使用的运行素材，共 1,396,214 字节：旧托盘、旧骰子、薄荷绿素材、4 个旧图标和重复 JPG 纹理。薄荷绿素材原已排除，真正减少待打包内容约 809 KiB。
- 删除仅重建旧版薄荷绿素材的脚本，当前图标构建脚本不再生成闲置图标或重复纹理；排除依赖清单和 Git 数据。
- 项目检查增加未使用素材检查和 1.8 MiB 编译前预算。本地待打包文件由约 2453 KiB 降至 1643.3 KiB（约 1.60 MiB）；这是保守的编译前估算，不是服务器源码包精确统计。

## 本轮验证

- `node --test tests/*.test.js`：最终 30/30 通过。拖动测试验证位移、松手停留、极限位置和反向拖动，容许状态量化造成的小于 0.2rpx 误差。
- `node scripts/check-project.js`：16 个素材引用、源码语法、结构、未使用素材与包体预算检查通过。
- 官方 `compile_wxml` / `compile_wxss` 返回 `success: true`。编译检查覆盖模板与样式，不单独代表整包上传。
- 原生模拟器检查过放大后的合盖、半开与全开。最终 340rpx 抬升另用当前样式与原始素材在 430×834、375×569、393×681 三种内容尺寸复核，每种检查 0%、10%、50%、100% 开度；杯顶出屏符合用户要求，骰面可完整查看。
- 12:39:51 的开发者工具日志记录真机调试包 `uploadFile progressSuccess`，随后创建 remote-debug 页面；本次清理后的包已通过上传。该上传发生在最终抬升距离调整之前，最终代码仍需重新生成真机调试包。
- 自动化真机命令返回 `timeout waiting for automator response`；读取已安装自动化库确认 remote 流程会等待手机连接，不能据此宣称手机端运行已通过。切入远程调试后模拟器自动化截图也超时，最终 340rpx 视觉记录属于离线复核。
- 本轮 console 的 error 检索无匹配；warn 检索有一次 `Page.onLoad took 279ms` 性能提示，不影响上述素材与包体判断。

本轮截图和复核数据保存在项目外的 `C:/Users/zhout/Documents/ChatGPT/dice-shaker_V1/verification/`，避免生成验收文件触发项目热重载。其中 `cup-qa-*-*.png`、`cup-viewport-check.json` 对应最终 340rpx 状态；较早的 `cup-final-*.jpg` 为中间方案，不能当作最终抬升高度证据。

以下内容为 2026-08-31 历史验收，保留供追溯；不代表当前实现。

---

# 五颗骰子排列恢复与杯盘放大 · 历史设计验收

final result: passed

日期：2026-08-31。最新范围：按用户追加图片恢复五颗骰子的排列，保持当前骰子的角度、大小和侧面细节，盖子与底座略微放大。仍为原生微信小程序，没有新建网页、上传或发布。

此前黑色设计、声音、锁定和骰面修订的完整验收保留在 `design/layout-return-20260831/previous-design-qa.md`。该历史报告中的五颗3+2排列已由本次用户选择的1+2+2替代，不能当作当前排列。以下截图路径均相对于 `design/layout-return-20260831/`。

## 参考与实现范围

- 用户原附件：`C:/Users/xiaojian/.codex/codex-remote-attachments/01a05731-0a51-7b92-92f9-b722284653a7/46A2CC28-7976-44CE-B2A0-93D0583A1C4B/1-照片-1.jpg`。只读；项目副本为 `reference.jpg`。
- 五颗中心位置恢复为：`[[50,14],[21,43],[79,43],[37,82],[64,82]]`。即后方一颗、左右各一颗、前方两颗。
- 六张骰子PNG、128rpx显示尺寸和变换均未改。六张SHA-256前后全部一致，见 `dice-hashes-before.json`、`dice-hash-verification.json`。保留新骰子俯视角及合法侧面点数，没有恢复参考中旧骰子素材。
- 盖子：464×564rpx → 492×598rpx；底座：530×324rpx → 562×343.6rpx；宽高同比放大约6%。杯底锚点相应调整，仍与底盘贴合。
- 为容纳更大的开盖高度，场景锚点略向下；短屏同步调整场景和开合提示位置。没有改变开盖位移、摇动动画、声音、锁定、随机数或设置逻辑；1～4及6颗的排列也没有改变。

## 同状态视觉对照

- 大屏：iPhone 15 Pro Max，430×932；小程序内容430×834逻辑像素，原生截图接口输出645×1251。`closed.png`、`open.png`均为真实模拟器截图。
- 参考图与开盖实现同为645×1251，未缩放地并排拼接，14px间隔：`reference-vs-current.png`。用户要求保留新骰子角度并放大杯盘，属于有意差异。
- `before-vs-current.png`左侧为上次已提交版本，右侧为本次实现。只恢复位置和放大杯盘，不改变骰子角度。
- 两份局部对照 `reference-vs-current-detail.png`、`before-vs-current-detail.png`均裁取x=80、y=540、w=490、h=350，保持相同像素尺度。
- 上述全景、局部已实际打开检查；拼接脚本为 `scripts/build-layout-comparison.js`，归一化参数见 `comparison-metadata.json`。没有用程序绘制骰面或合成UI。
- `preview-closed-open.png`是便于交付的等比缩小预览，左合盖、右开盖，不取代全尺寸验收。
- 小屏：iPhone 6/7/8，375×667；内容375×603，截图563×905。`small-closed.png`、`small-open.png`显示杯顶、盘沿、骰面、提示、主按钮与锁均完整可见。验证后已恢复原来的大屏机型。

## 发现、修复与复验

|轮次/级别|发现|处理与证据|
|---|---|---|
|本次P2|五颗3+2不符合用户新指定的参考排列|仅恢复五颗位置；完整及局部对照确认1+2+2，五个顶面均可辨认|
|本次P2|放大杯盘会压缩开盖后顶部及短屏提示的空间|同步调整场景与短屏提示锚点，大小屏开合截图确认未裁切、未重叠|
|验证过程|热重载最初保留了旧页面排列状态|执行完整编译后再抓图，最终 `open.png` 已显示正确排列；不将旧热重载画面充当最终证据|
|最终复验|没有未解决的P0/P1/P2问题|全景、局部和375×667实际操作已复验|

## 五项检查

|表面|结论|
|---|---|
|字体与排版|沿用原字体与字号；提示、摇一摇、设置和锁图标没有截断|
|间距与布局|五颗恢复参考位置关系，顶面没有互相遮挡；放大杯盘在大小屏开合状态完整|
|颜色与状态|灰底、黑色杯盘、白色骰子及红蓝点数未变；此次没有修改状态配色|
|图像与图标|全部复用现有本地素材；六张骰子哈希完全一致，角度、侧面点数和透明边缘保持原样|
|文案与内容|没有改变按钮文案、数量范围或交互语义；README同步描述当前五颗布局|

保留的P3：参考图使用旧骰子素材，当前侧面及俯视角与参考不同是用户明确要求保留的效果；杯面高光、纹理和字体抗锯齿沿用此前实现，不宣称逐像素复刻。

## 本次实际验证与边界

- 微信开发者工具 Stable v2.01.2510260、基础库3.17.2，原有AppID和基础库未改。
- 大小屏实际点击开盖；小屏真实下划手势合盖通过，放大的杯盘没有阻碍操作。
- 最后恢复大屏，保留五颗、未锁定的开盖效果供用户查看。最终稳定状态0 Errors；警告详情保留在 `console-final.txt` 和 `final-wechat-preview.png`。
- 切换机型时曾短暂显示旧尺寸并出现重新加载状态；等待重新编译完成后才检查和截图。没有把加载间隙画面当作通过证据。
- `npm test`：20通过、0失败；现有测试覆盖开合、随机点数、锁定取消、声音起止、手势及传感器检测。日志 `test-results.txt`。
- `npm run check`：通过，5个业务JS、17个素材引用、WXML和WXSS结构检查。日志 `check-results.txt`。`git diff --check`通过。
- 本次是局部布局修订，没有重跑此前13项原生Console集成测试，也没有声称本次重新实测了音频播放或实体手机晃动。原生集成和声音证据在历史验收中保留；真机听感、性能及晃动阈值仍未验收。
- 上次按用户要求创建的本地提交保留；本次改动留在工作区。没有推送远程仓库、上传小程序或更改账号、代理、凭据及安全设置。
